schema {
  query: Query
  mutation: Mutation
}

type User {
  id: ID!
  nickname: String!
  email: String
  externalId: String
  imageURL: String
}

type Query {
  me: User
}

type Mutation {
  createUser(nickname: String!): User!
}
